<div class="text-center">
  <h2>{{post}}</h2>
  
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span class="band-name">{{band.name}}</span>
    
  </p>
  <div>
    <textarea class="form-area" id="comment"></textarea>
  </div>
  <p>
    <button class="btn btn-primary">Comment</button>
  </p>
</div>
